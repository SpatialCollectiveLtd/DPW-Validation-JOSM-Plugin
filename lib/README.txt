Place third-party jars required for building (for example, JDatePicker jar) into this folder.

Suggested file:
- jdatepicker-x.x.x.jar

The build script will automatically include jars from this folder on the classpath.